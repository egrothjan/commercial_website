/// <reference types="styled-jsx" />

// Ensure TypeScript and the editor recognize the `jsx`/`global` attributes on <style> tags
// When using Next.js with `styled-jsx` these attributes are provided by the `styled-jsx` types.
